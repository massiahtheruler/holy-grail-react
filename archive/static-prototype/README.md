# Static Prototype Archive

These files are from the earlier non-React storefront phase of Holy Grails.

They are kept here only as historical reference for the pre-conversion build.
The active app lives in `src/` and is mounted through `src/main.jsx` and `src/RootApp.jsx`.

Archived here:

- `shop.html`
- `search.html`
- `app.js`
- `search.js`

These files are not part of the current React build.
